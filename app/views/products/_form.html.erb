<br/>
<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='row'>
    <div class="field small-2 columns">
      <%= f.label :name, class:'right' %>
    </div>
    <div class='field small-10 columns'>
      <%= f.text_field :name %>
    </div>
  </div>
  <div class='row'>
    <div class="field small-2 columns">
      <%= f.label :price, title: "Price in USD", data: {tooltip: true },
          class:'right' %>
    </div>
    <div class='field small-3 columns'>
      <%= f.text_field :price, class:'leftf' %>
    </div>
    <span class='small-7 columns'></span>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <br/>
<% end %>
<br/>
